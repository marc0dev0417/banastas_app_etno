import 'package:flutter/cupertino.dart';

class Menu {
  String? assetImage;
  String? title;
  String? webUrl;
  IconData? iconData;

  Menu(this.assetImage, this.title, this.webUrl, this.iconData);
}